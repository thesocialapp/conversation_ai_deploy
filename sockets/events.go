package socket

// OnConnect waits for a connection and then calls f().
func OnConnect(f func()) {
	// Implementation
}

// Emit emits the event with the given data.
func Emit(event string, data interface{}) {
	// Implementation
}
